{
  "name": "@ssthouse/tree-chart-core",
  "version": "1.2.0",
  "description": "tree chart's core logic (shared by Vue2,Vue3,React)",
  "main": "build/index.js",
  "module": "build/index.js",
  "scripts": {
    "build": "rimraf build && npx tsc --declaration true",
    "release": "npm run build && ../../scripts/publish-safe.sh"
  },
  "author": "ssthouse",
  "license": "ISC",
  "dependencies": {
    "build": "^0.1.4",
    "d3": "^7.2.0",
    "pnpm": "^10.22.0"
  },
  "devDependencies": {
    "eslint": "^8.4.1",
    "rimraf": "^6.1.0",
    "typescript": "^4.5.2"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
